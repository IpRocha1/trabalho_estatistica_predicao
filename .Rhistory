base = read.csv2("coleta_lagoa_peri.csv")
View(base)
View(base)
base = read.csv("coleta_lagoa_peri.csv")
View(base)
View(base)
View(base)
View(base)
# Ajuste da base de dados
base = dados %>% select(-Data)
# Carregue os pacotes
library(ggplot2)
library(dplyr)
library(corrplot)
# Instalação dos pacotes
install.packages(c("ggplot2", "dplyr", "corrplot", "car", "caret"))
install.packages(c("ggplot2", "dplyr", "corrplot", "car", "caret"))
# Carregando os pacotes
library(ggplot2)
library(dplyr)
library(corrplot)
library(car)
library(caret)
# Importação dos dados para análise
base = read.csv("coleta_lagoa_peri.csv")
# Ajuste da base de dados
base = dados %>% select(-Data)
# Ajuste da base de dados
base = base %>% select(-Data)
View(base)
View(base)
# Resumo estatístico
summary(base)
View(base)
View(base)
# Importação dos dados para análise
base = read.csv("coleta_lagoa_peri.csv", locale = locale(decimal_mark = ","))
# Ajuste da base de dados
base = base %>% select(-Data)
View(base)
View(base)
# Resumo estatístico
summary(base)
# Importação dos dados para análise
base = read.csv("coleta_lagoa_peri.csv", locale = locale(decimal_mark = ","))
# Converter colunas numéricas
base = base %>%
mutate(
Temp = as.numeric(Temp),
Cond = as.numeric(Cond),
Oxigenio = as.numeric(Oxigenio),
Profund = as.numeric(Profund),
Vel = as.numeric(Vel),
Rainfall = as.numeric(Rainfall)
)
# Converter colunas numéricas
base = base %>%
mutate(
Temp. = as.numeric(Temp),
Cond. = as.numeric(Cond),
Oxigenio = as.numeric(Oxigenio),
Profund. = as.numeric(Profund),
Vel = as.numeric(Vel),
Rainfall = as.numeric(Rainfall)
)
# Converter colunas numéricas
base = base %>%
mutate(
Temp. = as.numeric(Temp.),
Cond. = as.numeric(Cond.),
Oxigenio = as.numeric(Oxigenio),
Profund. = as.numeric(Profund.),
Vel = as.numeric(Vel),
Rainfall = as.numeric(Rainfall)
)
dplyr::last_dplyr_warnings()
# Importação dos dados para análise
base = read.csv("coleta_lagoa_peri.csv", locale = locale(decimal_mark = ","))
# Converter colunas numéricas
base = base %>%
mutate(
Temp. = as.numeric(Temp.),
Cond. = as.numeric(Cond.),
Oxigenio = as.numeric(Oxigenio),
Profund. = as.numeric(Profund.),
Vel = as.numeric(Vel),
Rainfall = as.numeric(Rainfall)
)
View(base)
View(base)
# Ajuste da base de dados
base = base %>% select(-Data)
View(base)
clear
clean
# Carregando os pacotes
library(ggplot2)
library(dplyr)
library(corrplot)
library(car)
library(caret)
# Importação dos dados para análise
base = read.csv("coleta_lagoa_peri.csv", locale = locale(decimal_mark = ","))
# Importação dos dados para análise
base = read.csv("coleta_lagoa_peri.csv", locale = locale(decimal_mark = ","))
# Importação dos dados para análise
base2 = read.csv("coleta_lagoa_peri.csv", locale = locale(decimal_mark = ","))
# Importação dos dados para análise
base2 = read.csv("coleta_lagoa_peri.csv", locale = locale(decimal_mark = ","))
View(base)
View(base)
# Instalação dos pacotes
install.packages(c("ggplot2", "dplyr", "corrplot", "car", "caret"))
# Carregando os pacotes
library(ggplot2)
library(dplyr)
library(corrplot)
library(car)
library(caret)
# Importação dos dados para análise
base2 = read.csv("coleta_lagoa_peri.csv", locale = locale(decimal_mark = ","))
